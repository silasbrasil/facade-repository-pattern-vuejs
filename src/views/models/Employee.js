
class Employee {
  constructor({
    id,
    employee_name,
    employee_salary,
    employee_age,
    profile_image
  }) {
    this.id = id
    this.name = employee_name
    this.salary = employee_salary
    this.age = employee_age
    this.image = profile_image
  }
}